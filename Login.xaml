<UserControl x:Class="GestionComerce.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:GestionComerce.Resources"
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3B82F6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NumericButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F9FAFB"/>
                                <Setter Property="BorderBrush" Value="#2563EB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#EFF6FF"/>
                                <Setter Property="BorderBrush" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ClearButtonStyle" TargetType="Button" BasedOn="{StaticResource NumericButtonStyle}">
            <Setter Property="Background" Value="#FEF2F2"/>
            <Setter Property="Foreground" Value="#DC2626"/>
            <Setter Property="BorderBrush" Value="#FECACA"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FEE2E2"/>
                                <Setter Property="BorderBrush" Value="#FCA5A5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FECACA"/>
                                <Setter Property="BorderBrush" Value="#F87171"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EnterButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#34D399"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#059669"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2563EB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#93C5FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Shared style for plain text inputs -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="CaretBrush" Value="#2563EB"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2563EB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#93C5FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DropShadowEffect x:Key="CardShadow" Color="#000000" Opacity="0.12"
                          BlurRadius="30" ShadowDepth="0" Direction="270"/>
        <DropShadowEffect x:Key="SubtleShadow" Color="#000000" Opacity="0.06"
                          BlurRadius="12" ShadowDepth="2" Direction="270"/>
    </UserControl.Resources>

    <Grid MouseLeftButtonDown="Card_MouseLeftButtonDown">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#EFF6FF" Offset="0"/>
                <GradientStop Color="#DBEAFE" Offset="0.5"/>
                <GradientStop Color="#BFDBFE" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Width="1200" Height="765" Background="White" CornerRadius="24"
                Effect="{StaticResource CardShadow}" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.3*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel -->
                <Grid Grid.Column="0">
                    <Border Background="#F9FAFB" CornerRadius="24,0,0,24" Opacity="0.5"/>
                    <StackPanel Margin="60,50" VerticalAlignment="Center">

                        <!-- Logo / company header -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,30">
                            <Border Width="70" Height="70" CornerRadius="18">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="Transparent" Offset="0"/>
                                        <GradientStop Color="Transparent" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.Effect>
                                    <DropShadowEffect Color="#2563EB" Opacity="0.3" BlurRadius="15" ShadowDepth="0"/>
                                </Border.Effect>
                                <Image Source="/Main/images/BM-Logo.ico" Margin="0,8,0,6"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center" Margin="20,0,0,0">
                                <TextBlock Text="{x:Static res:Resources.CompanyName}"
                                           FontFamily="Segoe UI" FontSize="26" FontWeight="SemiBold" Foreground="#111827"/>
                                <TextBlock Text="{x:Static res:Resources.CompanyHeadquarters}"
                                           FontFamily="Segoe UI" FontSize="26" FontWeight="SemiBold"
                                           Foreground="#111827" Margin="0,-5,0,0"/>
                                <TextBlock Text="{x:Static res:Resources.About_Tagline}"
                                           FontFamily="Segoe UI" FontSize="13" Foreground="#6B7280" Margin="0,5,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Text="{x:Static res:Resources.About_Tagline}"
                                   FontFamily="Segoe UI" FontSize="36" FontWeight="Light"
                                   Foreground="#111827" Margin="0,0,0,6"/>

                        <TextBlock Text="{x:Static res:Resources.General_Description}"
                                   FontFamily="Segoe UI" FontSize="15" Foreground="#6B7280"
                                   TextWrapping="Wrap" MaxWidth="400"
                                   HorizontalAlignment="Left" Margin="0,0,0,20"/>

                        <!-- ── 3 NEW INPUTS ── -->
                        <StackPanel Width="380" HorizontalAlignment="Left" Margin="0,0,0,20">

                            <!-- Username -->
                            <TextBlock Text="Username"
                                       FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                                       Foreground="#374151" Margin="0,0,0,8"/>
                            <TextBox x:Name="UsernameInput"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     MaxLength="64"/>

                            <!-- Password -->
                            <TextBlock Text="Password"
                                       FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                                       Foreground="#374151" Margin="0,16,0,8"/>
                            <PasswordBox x:Name="UserPasswordInput"
                                         Style="{StaticResource ModernPasswordBoxStyle}"/>

                            <!-- App Username -->
                            <TextBlock Text="App Username"
                                       FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                                       Foreground="#374151" Margin="0,16,0,8"/>
                            <TextBox x:Name="AppUsernameInput"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     MaxLength="64"/>

                        </StackPanel>
                        <!-- ── END 3 NEW INPUTS ── -->

                        <!-- ── ORIGINAL PIN — completely untouched ── -->
                        <StackPanel>
                            <TextBlock Text="{x:Static res:Resources.AddUser_Password}"
                                       FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                                       Foreground="#374151" Margin="0,0,0,10"/>
                            <PasswordBox x:Name="PasswordInput" MaxLength="9"
                                         Style="{StaticResource ModernPasswordBoxStyle}"
                                         Width="380" HorizontalAlignment="Left"
                                         PreviewTextInput="PasswordInput_PreviewTextInput"
                                         PreviewKeyDown="PasswordInput_PreviewKeyDown"/>
                            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                <Border Width="18" Height="18" Background="#DBEAFE" CornerRadius="9" Margin="0,0,8,0">
                                    <TextBlock Text="🔒" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="{x:Static res:Resources.About_AdvancedSecurity}"
                                           FontFamily="Segoe UI" FontSize="12" Foreground="#6B7280"/>
                            </StackPanel>
                        </StackPanel>
                        <!-- ── END ORIGINAL PIN ── -->

                    </StackPanel>
                </Grid>

                <!-- Right Panel - Numpad -->
                <Border Grid.Column="1" Background="#FAFBFC" CornerRadius="0,24,24,0" Padding="50,40">
                    <Grid>
                        <Button Width="44" Height="44" HorizontalAlignment="Right" VerticalAlignment="Top"
                                Margin="0,-20,-20,0" Content="⏻" Foreground="White"
                                FontSize="20" FontWeight="Bold" BorderThickness="0"
                                Cursor="Hand" x:Name="BtnShutdown" Click="BtnShutdown_Click">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#EF4444" Offset="0"/>
                                    <GradientStop Color="#DC2626" Offset="1"/>
                                </LinearGradientBrush>
                            </Button.Background>
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid>
                                        <Ellipse Fill="{TemplateBinding Background}">
                                            <Ellipse.Effect>
                                                <DropShadowEffect Color="#DC2626" Opacity="0.3" BlurRadius="12" ShadowDepth="0"/>
                                            </Ellipse.Effect>
                                        </Ellipse>
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{x:Static res:Resources.General_VirtualKeyboard}"
                                       FontFamily="Segoe UI" FontSize="24" FontWeight="SemiBold"
                                       Foreground="#111827" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="{x:Static res:Resources.About_AdvancedSecurity}"
                                       FontFamily="Segoe UI" FontSize="14" Foreground="#9CA3AF"
                                       HorizontalAlignment="Center" Margin="0,0,0,35"/>

                            <Grid Width="360" HorizontalAlignment="Center">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="70"/>
                                    <RowDefinition Height="70"/>
                                    <RowDefinition Height="70"/>
                                    <RowDefinition Height="70"/>
                                    <RowDefinition Height="75"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Button Content="1" Grid.Row="0" Grid.Column="0" Style="{StaticResource NumericButtonStyle}" Margin="5" x:Name="Btn1"/>
                                <Button Content="2" Grid.Row="0" Grid.Column="1" Style="{StaticResource NumericButtonStyle}" Margin="5" x:Name="Btn2"/>
                                <Button Content="3" Grid.Row="0" Grid.Column="2" Style="{StaticResource NumericButtonStyle}" Margin="5" x:Name="Btn3"/>
                                <Button Content="4" Grid.Row="1" Grid.Column="0" Style="{StaticResource NumericButtonStyle}" Margin="5" x:Name="Btn4"/>
                                <Button Content="5" Grid.Row="1" Grid.Column="1" Style="{StaticResource NumericButtonStyle}" Margin="5" x:Name="Btn5"/>
                                <Button Content="6" Grid.Row="1" Grid.Column="2" Style="{StaticResource NumericButtonStyle}" Margin="5" x:Name="Btn6"/>
                                <Button Content="7" Grid.Row="2" Grid.Column="0" Style="{StaticResource NumericButtonStyle}" Margin="5" x:Name="Btn7"/>
                                <Button Content="8" Grid.Row="2" Grid.Column="1" Style="{StaticResource NumericButtonStyle}" Margin="5" x:Name="Btn8"/>
                                <Button Content="9" Grid.Row="2" Grid.Column="2" Style="{StaticResource NumericButtonStyle}" Margin="5" x:Name="Btn9"/>

                                <Button Content="{x:Static res:Resources.Reset}" Grid.Row="3" Grid.Column="0"
                                        Style="{StaticResource ClearButtonStyle}" Margin="5" x:Name="BtnClear"/>
                                <Button Content="0" Grid.Row="3" Grid.Column="1"
                                        Style="{StaticResource NumericButtonStyle}" Margin="5" x:Name="Btn0"/>
                                <Button Content="⌫" Grid.Row="3" Grid.Column="2"
                                        Style="{StaticResource ClearButtonStyle}" Margin="5" x:Name="BtnDelete" FontSize="20"/>

                                <Button Content="{x:Static res:Resources.Confirm}" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3"
                                        Style="{StaticResource EnterButtonStyle}"
                                        Margin="5,10,5,5" x:Name="BtnEnter" Click="BtnEnter_Click">
                                    <Button.Effect>
                                        <DropShadowEffect Color="#10B981" Opacity="0.25" BlurRadius="15" ShadowDepth="0"/>
                                    </Button.Effect>
                                </Button>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
        <!-- Windowed / Full-Screen toggle — floats top-right over the whole page -->
        <Button x:Name="WinStateBtn" Height="48" HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="0,28,28,0" Cursor="Hand" Click="WinStateBtn_Click" BorderThickness="0">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="btnBorder" CornerRadius="24" Padding="22,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#3B82F6" Offset="0"/>
                                <GradientStop Color="#2563EB" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect Color="#2563EB" BlurRadius="18" ShadowDepth="0" Opacity="0.40"/>
                        </Border.Effect>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="WinStateIcon"
                                       Text="&#xE923;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="17" Foreground="White"
                                       VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock x:Name="WinStateLabel"
                                       Text="Fenêtré"
                                       FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                                       Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="btnBorder" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#60A5FA" Offset="0"/>
                                        <GradientStop Color="#3B82F6" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="btnBorder" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#1D4ED8" Offset="0"/>
                                        <GradientStop Color="#1E40AF" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

    </Grid>
</UserControl>

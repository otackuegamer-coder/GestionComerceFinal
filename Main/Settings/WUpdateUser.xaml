<Window x:Class="GestionComerce.Main.Settings.WUpdateUser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:GestionComerce.Resources"
        Title="{x:Static res:Resources.UpdateUser_Title}" Height="550" Width="520"
        Background="White" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter Property="Opacity" Value="0.9"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.Effect>
        <DropShadowEffect ShadowDepth="0" BlurRadius="20" Opacity="0.3"/>
    </Window.Effect>

    <Border Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1">
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,35">
                <TextBlock Text="{x:Static res:Resources.UpdateUser_Title}" FontWeight="Bold" FontSize="24" Foreground="#111827" Margin="0,0,0,8"/>
                <TextBlock Text="{x:Static res:Resources.UpdateUser_Description}" FontSize="14" Foreground="#6B7280"/>
            </StackPanel>

            <!-- Form -->
            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="{x:Static res:Resources.AddUser_FullName}" FontWeight="SemiBold" FontSize="14" Foreground="#374151" Margin="0,0,0,8"/>
                    <TextBox x:Name="Name" Style="{StaticResource InputTextBoxStyle}" MaxLength="100"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="{x:Static res:Resources.AddUser_Password}" FontWeight="SemiBold" FontSize="14" Foreground="#374151" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="Code" Style="{StaticResource InputPasswordBoxStyle}" 
                                 PreviewTextInput="PasswordInput_PreviewTextInput" MaxLength="9"
                                 PreviewKeyDown="PasswordInput_PreviewKeyDown"/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="{x:Static res:Resources.AddUser_Role}" FontWeight="SemiBold" FontSize="14" Foreground="#374151" Margin="0,0,0,8"/>
                    <ComboBox x:Name="Roles"/>
                </StackPanel>
            </StackPanel>

            <!-- Action Buttons -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="CancelButton" Content="{x:Static res:Resources.AddUser_Cancel}" 
                        Height="48" FontSize="15" FontWeight="SemiBold"
                        Background="#F3F4F6" Foreground="#374151" BorderThickness="0" 
                        Style="{StaticResource ModernButtonStyle}" Click="CancelButton_Click"/>
                <Button Grid.Column="2" x:Name="RegisterButton" Content="{x:Static res:Resources.UpdateUser_Apply}" 
                        Height="48" FontSize="15" FontWeight="SemiBold"
                        Background="#3B82F6" Foreground="White" BorderThickness="0" 
                        Style="{StaticResource ModernButtonStyle}" Click="RegisterButton_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>

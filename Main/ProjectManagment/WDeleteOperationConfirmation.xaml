<Window x:Class="GestionComerce.Main.ProjectManagment.WDeleteOperationConfirmation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Supprimer l'opération" Height="371" Width="460"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B91C1C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#991B1B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F9FAFB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="16" Margin="20">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="0" BlurRadius="40" Opacity="0.15"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#FEF2F2" CornerRadius="16,16,0,0"
                    Padding="24,20" BorderThickness="0,0,0,1" BorderBrush="#FEE2E2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Trash icon -->
                    <Border Grid.Column="0" Width="40" Height="40" Background="#FEE2E2"
                            CornerRadius="20" Margin="0,0,16,0">
                        <Viewbox Width="20" Height="20">
                            <Canvas Width="24" Height="24">
                                <Path Data="M3 6h18M8 6V4h8v2M19 6l-1 14H6L5 6"
                                      Stroke="#DC2626" StrokeThickness="2"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      Fill="Transparent"/>
                            </Canvas>
                        </Viewbox>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Supprimer l'opération"
                                   FontSize="18" FontWeight="Bold" Foreground="#991B1B" Margin="0,0,0,4"/>
                        <TextBlock Text="Cette action est irréversible"
                                   FontSize="13" Foreground="#DC2626" Opacity="0.8"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="24,24,24,24" VerticalAlignment="Center">
                <TextBlock Text="Êtes-vous sûr de vouloir supprimer cette opération ?"
                           FontSize="15" Foreground="#374151" TextWrapping="Wrap"
                           HorizontalAlignment="Center" TextAlignment="Center" LineHeight="22"/>
                <Border Background="#FEF3C7" CornerRadius="8" Padding="12,10" Margin="0,14,0,0"
                        BorderBrush="#FDE68A" BorderThickness="1">
                    <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="#92400E" LineHeight="20">
                        <Run FontWeight="Bold">⚠ Attention : </Run>
                        <Run>Tous les articles de cette opération seront reversés et supprimés et l'opération sera marquée comme supprimée.</Run>
                    </TextBlock>
                </Border>
            </StackPanel>

            <!-- Buttons -->
            <Border Grid.Row="2" Background="#FAFAFA" CornerRadius="0,0,16,16"
                    Padding="24,20" BorderThickness="0,1,0,0" BorderBrush="#F3F4F6">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Annuler"
                            Style="{StaticResource CancelButton}"
                            Click="CancelButton_Click"
                            Margin="0,0,12,0" Width="110"/>
                    <Button x:Name="ConfirmBtn"
                            Content="Supprimer"
                            Style="{StaticResource DangerButton}"
                            Click="ConfirmButton_Click"
                            Width="120"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>

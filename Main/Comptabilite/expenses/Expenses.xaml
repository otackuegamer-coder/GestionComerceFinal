<UserControl x:Class="Superete.Main.Comptabilite.Expenses"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:p="clr-namespace:GestionComerce.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F8F9FA">

    <UserControl.Resources>
        <!-- Modern Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="20" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- FIXED: Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E0E7FF"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="CaretBrush" Value="#1F2937"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          Focusable="False"
                                          Background="Transparent"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366F1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#A5B4FC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- FIXED: Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E0E7FF"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Named visual border, separate from ToggleButton -->
                            <Border x:Name="border"
                                    Background="White"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10"/>

                            <!-- ToggleButton is fully transparent - only handles click + arrow -->
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <Path x:Name="Arrow"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,12,0"
                                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                                  Fill="#6B7280"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Arrow" Property="Fill" Value="#6366F1"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="14,0,30,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              TextBlock.Foreground="#1F2937"/>

                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Center"
                                     Margin="14,0,30,0"
                                     Padding="0"
                                     Focusable="True"
                                     Background="Transparent"
                                     Foreground="#1F2937"
                                     CaretBrush="#1F2937"
                                     BorderThickness="0"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>

                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="White"
                                            BorderThickness="1"
                                            BorderBrush="#E5E7EB"
                                            CornerRadius="8"
                                            Margin="0,4,0,0">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                                        </Border.Effect>
                                    </Border>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#A5B4FC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366F1"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366F1"/>
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter Property="IsTabStop" Value="False"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                                Background="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E0E7FF"/>
                                <Setter Property="Foreground" Value="#6366F1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- FIXED: Modern DatePicker Style -->
        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E0E7FF"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox"
                                                   Grid.Column="0"
                                                   Margin="14,0,0,0"
                                                   VerticalAlignment="Center"
                                                   Foreground="#1F2937"
                                                   CaretBrush="#1F2937"
                                                   Background="Transparent"
                                                   BorderThickness="0"
                                                   Focusable="True">
                                    <DatePickerTextBox.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#9CA3AF"/>
                                        </Style>
                                    </DatePickerTextBox.Resources>
                                </DatePickerTextBox>
                                <Button x:Name="PART_Button"
                                        Grid.Column="1"
                                        Width="40"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Focusable="False">
                                    <Path Data="M19 4h-1V2h-2v2H8V2H6v2H5c-1.11 0-1.99.9-1.99 2L3 20c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V10h14v10zm0-12H5V6h14v2z"
                                          Fill="#6B7280"
                                          Stretch="Uniform"
                                          Width="16"
                                          Height="16"/>
                                </Button>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#A5B4FC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#6366F1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Padding" Value="28,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4F46E5"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#C7D2FE"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Success Button Style -->
        <Style x:Key="SuccessButton" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Edit Button Style -->
        <Style x:Key="EditButton" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Padding" Value="24,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5E7EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Section Title Style -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <!-- Status Badge Style -->
        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <Grid MaxWidth="1400">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Page Header -->
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{x:Static p:Resources.Expenses_Title}" 
                               FontSize="28" 
                               FontWeight="Bold"
                               Foreground="#111827"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="{x:Static p:Resources.Expenses_Subtitle}"
                               FontSize="14"
                               Foreground="#6B7280"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="{x:Static p:Resources.Expenses_AlertSettings}"
                        Style="{StaticResource SecondaryButton}"
                        Click="BtnAlertSettings_Click"
                        VerticalAlignment="Center"/>
            </Grid>

            <!-- Alert Notification Settings Card -->
            <Border Grid.Row="1" 
                    x:Name="AlertSettingsCard"
                    Style="{StaticResource CardStyle}"
                    Background="#FEF3C7"
                    BorderBrush="#F59E0B"
                    BorderThickness="2"
                    Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                               Text="{x:Static p:Resources.Expenses_AlertConfig}" 
                               Style="{StaticResource SectionTitle}"
                               Foreground="#92400E"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,15,0">
                            <TextBlock Text="{x:Static p:Resources.Expenses_AlertDelay}" Style="{StaticResource LabelStyle}" Foreground="#92400E"/>
                            <TextBox x:Name="txtAlertValue" 
                                     Style="{StaticResource ModernTextBox}"
                                     Text="5"
                                     ToolTip="Entrez une valeur numérique"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="15,0,15,0">
                            <TextBlock Text="{x:Static p:Resources.Expenses_TimeUnit}" Style="{StaticResource LabelStyle}" Foreground="#92400E"/>
                            <ComboBox x:Name="cmbAlertUnit" 
                                      Style="{StaticResource ModernComboBox}"
                                      SelectedIndex="1">
                                <ComboBoxItem Content="{x:Static p:Resources.Expenses_Hours}" Tag="hours"/>
                                <ComboBoxItem Content="{x:Static p:Resources.Expenses_DaysUnit}" Tag="days"/>
                                <ComboBoxItem Content="{x:Static p:Resources.Expenses_Weeks}" Tag="weeks"/>
                            </ComboBox>
                        </StackPanel>

                        <Button Grid.Column="2"
                                Content="{x:Static p:Resources.Expenses_SaveBtn}"
                                Style="{StaticResource PrimaryButton}"
                                Click="BtnSaveAlertSettings_Click"
                                VerticalAlignment="Bottom"/>
                    </Grid>
                </Grid>
            </Border>

            <!-- Alert Banner for Upcoming Payments -->
            <Border Grid.Row="2" 
                    x:Name="AlertBanner"
                    Style="{StaticResource CardStyle}"
                    Background="#FEE2E2"
                    BorderBrush="#EF4444"
                    BorderThickness="2"
                    Visibility="Collapsed">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="{x:Static p:Resources.Expenses_UpcomingPayments}" 
                                   FontSize="18" 
                                   FontWeight="SemiBold"
                                   Foreground="#991B1B"/>

                        <TextBlock Grid.Column="1"
                                   x:Name="txtAlertInfo"
                                   FontSize="13"
                                   Foreground="#7F1D1D"
                                   VerticalAlignment="Center"/>
                    </Grid>

                    <ItemsControl x:Name="AlertList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" 
                                        CornerRadius="8" 
                                        Padding="16,12" 
                                        Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding ExpenseName}" 
                                                       FontWeight="SemiBold"
                                                       FontSize="14"
                                                       Foreground="#111827"
                                                       Margin="0,0,0,4"/>
                                            <TextBlock Foreground="#6B7280" FontSize="13">
                                                <Run Text="{x:Static p:Resources.Expenses_CategoryLabel}"/>
                                                <Run Text="{Binding Category}" FontWeight="Medium"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Amount, StringFormat='{}{0:N2} MAD'}"
                                                   FontSize="16"
                                                   FontWeight="Bold"
                                                   Foreground="#DC2626"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,20,0"/>

                                        <Border Grid.Column="2" 
                                                Background="#FEE2E2"
                                                CornerRadius="6"
                                                Padding="12,6"
                                                VerticalAlignment="Center">
                                            <TextBlock Foreground="#991B1B" 
                                                       FontWeight="SemiBold"
                                                       FontSize="13">
                                                <Run Text="{x:Static p:Resources.Expenses_In}"/>
                                                <Run Text="{Binding DaysUntilDue, Mode=OneWay}"/>
                                                <Run Text="{x:Static p:Resources.Expenses_DaysSuffix}"/>
                                            </TextBlock>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Add New Expense Form -->
            <Border Grid.Row="3" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                               Text="{x:Static p:Resources.Expenses_AddNewTitle}" 
                               Style="{StaticResource SectionTitle}"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Row 1 -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,16">
                            <TextBlock Text="{x:Static p:Resources.Expenses_NameRequired}" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="txtExpenseName" 
                                     Style="{StaticResource ModernTextBox}"
                                     ToolTip="Ex: Loyer de bureau"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="12,0,12,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{x:Static p:Resources.Expenses_CategoryRequired}" Style="{StaticResource LabelStyle}"/>
                                <Button Grid.Column="1" 
                                        Content="+"
                                        Width="24" Height="24"
                                        FontSize="16"
                                        FontWeight="Bold"
                                        Background="#6366F1"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        ToolTip="{x:Static p:Resources.Expenses_AddCategoryTooltip}"
                                        Click="BtnAddCategory_Click">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#4F46E5"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                            <ComboBox x:Name="cmbCategory" 
                                      Style="{StaticResource ModernComboBox}"
                                      IsEditable="True"
                                      IsTextSearchEnabled="True"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="2" Margin="12,0,0,16">
                            <TextBlock Text="{x:Static p:Resources.Expenses_AmountRequired}" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="txtAmount" 
                                     Style="{StaticResource ModernTextBox}"
                                     ToolTip="Ex: 5000.00"
                                     PreviewTextInput="TxtAmount_PreviewTextInput"/>
                        </StackPanel>

                        <!-- Row 2 -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,16">
                            <TextBlock Text="{x:Static p:Resources.Expenses_DueDateRequired}" Style="{StaticResource LabelStyle}"/>
                            <DatePicker x:Name="dpDueDate" 
                                        Style="{StaticResource ModernDatePicker}"
                                        SelectedDateFormat="Short"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="12,0,12,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{x:Static p:Resources.Expenses_RecurrenceType}" Style="{StaticResource LabelStyle}"/>
                                <Button Grid.Column="1" 
                                        Content="+"
                                        Width="24" Height="24"
                                        FontSize="16"
                                        FontWeight="Bold"
                                        Background="#6366F1"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        ToolTip="{x:Static p:Resources.Expenses_AddRecurrenceTooltip}"
                                        Click="BtnAddRecurrence_Click">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#4F46E5"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                            <ComboBox x:Name="cmbRecurring" 
                                      Style="{StaticResource ModernComboBox}"
                                      IsEditable="True"
                                      SelectedIndex="0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="2" Margin="12,0,0,16">
                            <TextBlock Text="{x:Static p:Resources.Expenses_PaymentMethod}" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="cmbPaymentMethod" 
                                      Style="{StaticResource ModernComboBox}"
                                      SelectedIndex="0">
                                <ComboBoxItem Content="Cash"/>
                                <ComboBoxItem Content="{x:Static p:Resources.Expenses_CreditCard}"/>
                                <ComboBoxItem Content="{x:Static p:Resources.Expenses_Transfer}"/>
                                <ComboBoxItem Content="{x:Static p:Resources.Expenses_Cheque}"/>
                                <ComboBoxItem Content="Mobile Money"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Row 3 -->
                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,16">
                            <TextBlock Text="{x:Static p:Resources.EditExpense_NotesLabel}" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="txtNotes" 
                                     Height="90" 
                                     TextWrapping="Wrap" 
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     Style="{StaticResource ModernTextBox}"
                                     VerticalContentAlignment="Top"
                                     Padding="14,12"
                                     ToolTip="{x:Static p:Resources.Expenses_NotesTooltip}"/>
                        </StackPanel>

                        <!-- Row 4 - Action Buttons -->
                        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" 
                                    Orientation="Horizontal" 
                                    HorizontalAlignment="Right">
                            <Button Content="{x:Static p:Resources.Expenses_Reset}"
                                    Style="{StaticResource SecondaryButton}"
                                    Click="BtnClearForm_Click"
                                    Margin="0,0,12,0"/>
                            <Button Content="{x:Static p:Resources.Expenses_AddButton}"
                                    Style="{StaticResource PrimaryButton}"
                                    Click="BtnAddExpense_Click"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <!-- Expenses List -->
            <Border Grid.Row="4" Style="{StaticResource CardStyle}" MinHeight="400">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="{x:Static p:Resources.Expenses_ListTitle}" 
                                   Style="{StaticResource SectionTitle}"/>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="{x:Static p:Resources.Expenses_FilterButton}"
                                    Style="{StaticResource SecondaryButton}"
                                    Height="40"
                                    Margin="0,0,8,0"
                                    Click="BtnFilter_Click"/>
                            <Button Content="{x:Static p:Resources.Expenses_RefreshButton}"
                                    Style="{StaticResource SecondaryButton}"
                                    Height="40"
                                    Click="BtnRefresh_Click"/>
                        </StackPanel>
                    </Grid>

                    <!-- Filter Panel -->
                    <Border Grid.Row="1" 
                            x:Name="FilterPanel"
                            Background="#F9FAFB"
                            CornerRadius="10"
                            Padding="16"
                            Margin="0,0,0,16"
                            Visibility="Collapsed">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                <TextBlock Text="{x:Static p:Resources.Expenses_StatusFilter}" Style="{StaticResource LabelStyle}"/>
                                <ComboBox x:Name="cmbFilterStatus" 
                                          Style="{StaticResource ModernComboBox}"
                                          Height="40">
                                    <ComboBoxItem Content="{x:Static p:Resources.Expenses_AllStatus}" IsSelected="True"/>
                                    <ComboBoxItem Content="Pending"/>
                                    <ComboBoxItem Content="Paid"/>
                                    <ComboBoxItem Content="Overdue"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="12,0,12,0">
                                <TextBlock Text="{x:Static p:Resources.Expenses_CategoryFilter}" Style="{StaticResource LabelStyle}"/>
                                <ComboBox x:Name="cmbFilterCategory" 
                                          Style="{StaticResource ModernComboBox}"
                                          Height="40"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="12,0,12,0">
                                <TextBlock Text="{x:Static p:Resources.Expenses_PeriodFilter}" Style="{StaticResource LabelStyle}"/>
                                <ComboBox x:Name="cmbFilterPeriod" 
                                          Style="{StaticResource ModernComboBox}"
                                          Height="40"
                                          SelectedIndex="0">
                                    <ComboBoxItem Content="{x:Static p:Resources.Expenses_AllStatus}"/>
                                    <ComboBoxItem Content="{x:Static p:Resources.Expenses_ThisMonth}"/>
                                    <ComboBoxItem Content="{x:Static p:Resources.Expenses_ThisQuarter}"/>
                                    <ComboBoxItem Content="{x:Static p:Resources.Expenses_ThisYear}"/>
                                </ComboBox>
                            </StackPanel>

                            <Button Grid.Column="3"
                                    Content="{x:Static p:Resources.Expenses_ApplyFilter}"
                                    Style="{StaticResource PrimaryButton}"
                                    Height="40"
                                    VerticalAlignment="Bottom"
                                    Click="BtnApplyFilter_Click"/>
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="2"
                              x:Name="dgExpenses"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              BorderThickness="0"
                              Background="Transparent"
                              RowBackground="White"
                              AlternatingRowBackground="#F9FAFB"
                              FontSize="13"
                              RowHeight="60"
                              SelectionMode="Single"
                              IsReadOnly="True">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="#374151"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Padding" Value="16,12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                <Setter Property="BorderBrush" Value="#E5E7EB"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="16,8"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}" 
                                                    Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{x:Static p:Resources.Expenses_ColName}" 
                                                Binding="{Binding ExpenseName}" 
                                                Width="*" 
                                                MinWidth="150">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Medium"/>
                                        <Setter Property="Foreground" Value="#111827"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="{x:Static p:Resources.Expenses_ColCategory}" 
                                                Binding="{Binding Category}" 
                                                Width="140">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#6B7280"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="{x:Static p:Resources.Expenses_ColAmount}" 
                                                Binding="{Binding Amount, StringFormat='{}{0:N2} MAD'}" 
                                                Width="130">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="#DC2626"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="{x:Static p:Resources.Expenses_ColDueDate}" 
                                                Binding="{Binding DueDate, StringFormat='dd/MM/yyyy'}" 
                                                Width="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#6B7280"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="{x:Static p:Resources.Expenses_ColStatus}" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border>
                                            <Border.Style>
                                                <Style TargetType="Border" BasedOn="{StaticResource StatusBadge}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding PaymentStatus}" Value="Paid">
                                                            <Setter Property="Background" Value="#D1FAE5"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PaymentStatus}" Value="Pending">
                                                            <Setter Property="Background" Value="#FEF3C7"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PaymentStatus}" Value="Overdue">
                                                            <Setter Property="Background" Value="#FEE2E2"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding PaymentStatus}" 
                                                       FontSize="12"
                                                       FontWeight="SemiBold"
                                                       HorizontalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding PaymentStatus}" Value="Paid">
                                                                <Setter Property="Foreground" Value="#065F46"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding PaymentStatus}" Value="Pending">
                                                                <Setter Property="Foreground" Value="#92400E"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding PaymentStatus}" Value="Overdue">
                                                                <Setter Property="Foreground" Value="#991B1B"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="{x:Static p:Resources.Expenses_ColRecurrence}" 
                                                Binding="{Binding RecurringType}" 
                                                Width="110">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#6B7280"/>
                                        <Setter Property="FontSize" Value="12"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="{x:Static p:Resources.Expenses_ColActions}" Width="220">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" 
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Margin="4,0">
                                            <Button Content="{x:Static p:Resources.Expenses_Pay}" 
                                                    Style="{StaticResource SuccessButton}"
                                                    Margin="0,0,4,0"
                                                    Click="BtnPayExpense_Click"
                                                    Tag="{Binding}"/>
                                            <Button Content="{x:Static p:Resources.Expenses_EditBtn}" 
                                                    Style="{StaticResource EditButton}"
                                                    Margin="0,0,4,0"
                                                    Click="BtnEditExpense_Click"
                                                    Tag="{Binding}"/>
                                            <Button Content="🗑️" 
                                                    Style="{StaticResource DangerButton}"
                                                    ToolTip="{x:Static p:Resources.Expenses_DeleteTooltip}"
                                                    Click="BtnDeleteExpense_Click"
                                                    Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
<UserControl x:Class="GestionComerce.Main.Facturation.FacturesEnregistrees.CMainEnregistrees"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:res="clr-namespace:GestionComerce.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#F9FAFB">
    <UserControl.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2563EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#DC2626"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Success Button Style -->
        <Style x:Key="SuccessButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#059669"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Purple Button Style — for Articles action -->
        <Style x:Key="ArticleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#8B5CF6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#7C3AED"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#C4B5FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True"
                                Padding="12,0">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Focusable="false"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F3F4F6"/>
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Modern DatePicker Style -->
        <Style x:Key="ModernDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Article count badge style -->
        <Style x:Key="ArticleBadgeStyle" TargetType="Border">
            <Setter Property="Background" Value="#EDE9FE"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="0"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="{x:Static res:Resources.SavedInvoicesSuppliers}" 
                               FontSize="24" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,8"/>
                    <TextBlock Grid.Row="1" Text="{x:Static res:Resources.SavedInvoicesDescription}" 
                               FontSize="14" Foreground="#6B7280"/>
                </Grid>
            </Border>

            <!-- Add/Edit Form -->
            <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="24" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="0"/>
                </Border.Effect>
                <StackPanel>
                    <!-- Form Title -->
                    <TextBlock Text="{x:Static res:Resources.AddModifySupplierInvoice}" 
                               FontSize="18" FontWeight="SemiBold" Foreground="#111827" Margin="0,0,0,20"/>

                    <!-- Row 1: Reference, Supplier, Amount, Date -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Static res:Resources.InvoiceReference}" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtReference" Style="{StaticResource ModernTextBoxStyle}" Height="40"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="{x:Static res:Resources.SupplierRequired}" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <ComboBox x:Name="cmbSupplier" Style="{StaticResource ModernComboBoxStyle}" 
                                      Height="40" DisplayMemberPath="DisplayText" SelectedValuePath="FournisseurID"/>
                        </StackPanel>

                        <StackPanel Grid.Column="4">
                            <TextBlock Text="{x:Static res:Resources.TotalAmountMAD}" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtAmount" Style="{StaticResource ModernTextBoxStyle}" Height="40"/>
                        </StackPanel>

                        <StackPanel Grid.Column="6">
                            <TextBlock Text="{x:Static res:Resources.InvoiceDate}" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <DatePicker x:Name="dpInvoiceDate" Style="{StaticResource ModernDatePickerStyle}" 
                                        Height="40" BorderBrush="#D1D5DB" BorderThickness="1"/>
                        </StackPanel>
                    </Grid>

                    <!-- Row 2: Image Upload -->
                    <Grid Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="{x:Static res:Resources.InvoiceImage}" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="txtImagePath" Style="{StaticResource ModernTextBoxStyle}" 
                                         Grid.Column="0" IsReadOnly="True" Height="40"/>
                                <Button Grid.Column="2" Content="{x:Static res:Resources.Browse}" Style="{StaticResource SecondaryButtonStyle}" 
                                        Click="BrowseImage_Click" Padding="12,10" Height="40" Width="120"/>
                                <Button Grid.Column="4" Content="{x:Static res:Resources.Preview}" Style="{StaticResource ModernButtonStyle}" 
                                        Click="PreviewImage_Click" Padding="12,10" Height="40" Width="120"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <!-- Row 3: Description (Full Width) -->
                    <Grid Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="{x:Static res:Resources.Description}" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtDescription" Style="{StaticResource ModernTextBoxStyle}" 
                                     Height="60" TextWrapping="Wrap" AcceptsReturn="True" 
                                     VerticalScrollBarVisibility="Auto"/>
                        </StackPanel>
                    </Grid>

                    <!-- Row 4: Notes (Full Width) -->
                    <Grid Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="{x:Static res:Resources.Notes}" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtNotes" Style="{StaticResource ModernTextBoxStyle}" 
                                     Height="60" TextWrapping="Wrap" AcceptsReturn="True" 
                                     VerticalScrollBarVisibility="Auto"/>
                        </StackPanel>
                    </Grid>

                    <!-- Action Buttons -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <!-- articles button (left) -->
                            <ColumnDefinition Width="*"/>
                            <!-- spacer -->
                            <ColumnDefinition Width="Auto"/>
                            <!-- right buttons -->
                        </Grid.ColumnDefinitions>

                        <!-- LEFT: Add Articles button — only visible when editing an existing invoice -->
                        <Button x:Name="btnManageArticles"
                                Grid.Column="0"
                                Content="📋 Gérer les Articles"
                                Style="{StaticResource ArticleButtonStyle}"
                                Width="180"
                                Visibility="Collapsed"
                                Click="ManageArticles_Click"/>

                        <!-- RIGHT: standard CRUD buttons -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="btnAdd" Content="{x:Static res:Resources.Register}" Style="{StaticResource ModernButtonStyle}" 
                                    Click="AddInvoice_Click" Margin="0,0,10,0" Width="150"/>
                            <Button x:Name="btnUpdate" Content="{x:Static res:Resources.Modify}" Style="{StaticResource SuccessButtonStyle}" 
                                    Click="UpdateInvoice_Click" Margin="0,0,10,0" Width="130" Visibility="Collapsed"/>
                            <Button x:Name="btnDelete" Content="{x:Static res:Resources.Delete}" Style="{StaticResource DangerButtonStyle}" 
                                    Click="DeleteInvoice_Click" Margin="0,0,10,0" Width="130" Visibility="Collapsed"/>
                            <Button Content="{x:Static res:Resources.Reset}" Style="{StaticResource SecondaryButtonStyle}" 
                                    Click="ResetForm_Click" Width="150"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- DataGrid Section -->
            <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="24" MinHeight="400">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="0"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Search Section -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0">
                            <TextBox x:Name="txtSearch" 
                                     Style="{StaticResource ModernTextBoxStyle}" 
                                     Height="40"
                                     TextChanged="Search_TextChanged"/>
                            <TextBlock IsHitTestVisible="False" Text="{x:Static res:Resources.SearchByReferenceOrDescription}" 
                                       Margin="16,0,0,0"
                                       VerticalAlignment="Center" HorizontalAlignment="Left" 
                                       Foreground="#9CA3AF" FontSize="14">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>

                        <Button Grid.Column="2" Content="{x:Static res:Resources.Search}" 
                                Style="{StaticResource ModernButtonStyle}" 
                                Click="Search_Click" Width="140" Height="40"/>

                        <ComboBox x:Name="cmbFilterSupplier" Grid.Column="4" 
                                  Style="{StaticResource ModernComboBoxStyle}" 
                                  Height="40" SelectionChanged="FilterSupplier_Changed"
                                  DisplayMemberPath="DisplayText" SelectedValuePath="FournisseurID">
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Padding" Value="10,8"/>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                        </ComboBox>
                    </Grid>

                    <!-- Results Count -->
                    <TextBlock Grid.Row="1" x:Name="txtResultsCount" 
                               Text="{x:Static res:Resources.Loading}" 
                               FontSize="13" Foreground="#6B7280" Margin="0,0,0,12"/>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="2" x:Name="dgInvoices" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              SelectionMode="Single"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserResizeRows="False"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              Background="White"
                              BorderThickness="1"
                              BorderBrush="#E5E7EB"
                              RowHeight="50"
                              FontSize="13"
                              SelectionChanged="dgInvoices_SelectionChanged"
                              MouseDoubleClick="dgInvoices_MouseDoubleClick">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F9FAFB"/>
                                <Setter Property="Foreground" Value="#111827"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Padding" Value="12,10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                <Setter Property="BorderBrush" Value="#E5E7EB"/>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F3F4F6"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#DBEAFE"/>
                                        <Setter Property="Foreground" Value="#1E40AF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="12,0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Background="{TemplateBinding Background}" 
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding SavedInvoiceID}" Width="55"/>
                            <DataGridTextColumn Header="{x:Static res:Resources.Reference}" Binding="{Binding InvoiceReference}" Width="130"/>
                            <DataGridTextColumn Header="{x:Static res:Resources.SupplierID}" Binding="{Binding FournisseurID}" Width="100"/>
                            <DataGridTextColumn Header="{x:Static res:Resources.Amount}" Binding="{Binding TotalAmount, StringFormat='{}{0:N2} MAD'}" Width="120"/>
                            <DataGridTextColumn Header="{x:Static res:Resources.Date}" Binding="{Binding InvoiceDate, StringFormat='dd/MM/yyyy'}" Width="100"/>
                            <DataGridTextColumn Header="{x:Static res:Resources.Description}" Binding="{Binding Description}" Width="*" MinWidth="140"/>

                            <!-- ── NEW: article count badge column ── -->
                            <DataGridTemplateColumn Header="Articles" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource ArticleBadgeStyle}">
                                            <TextBlock Text="{Binding ArticleCountLabel}"
                                                       FontSize="12" FontWeight="SemiBold"
                                                       Foreground="#7C3AED"
                                                       HorizontalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- ── NEW: view articles button column ── -->
                            <DataGridTemplateColumn Header="" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="📋 Articles"
                                                Style="{StaticResource ArticleButtonStyle}"
                                                Padding="8,4" FontSize="11"
                                                Click="ViewArticles_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Image view column -->
                            <DataGridTemplateColumn Header="{x:Static res:Resources.Image}" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="{x:Static res:Resources.ViewImage}" 
                                                Style="{StaticResource SecondaryButtonStyle}" 
                                                Padding="8,4" FontSize="11"
                                                Click="ViewImage_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
